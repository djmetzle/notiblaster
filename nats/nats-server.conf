# NATS server config for the notiblaster demo.
# Enables JetStream (persistent streams) and a WebSocket listener so
# browsers can connect directly via nats.ws.

port: 4222
http_port: 8222

jetstream {
  store_dir: "/data/jetstream"
}

websocket {
  port: 8080
  no_tls: true
}
